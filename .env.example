# Auth (optional): protect API endpoints
PYFLOWS_API_TOKEN=

# Enable kernel feature (override license gating for dev)
PYFLOWS_ENABLE_KERNEL=1

# Persist flows inside container to /data/flows
PYFLOWS_DATA_DIR=/data/flows

# Optional: use a remote Jupyter Enterprise Gateway
# JUPYTER_GATEWAY_URL=https://<gateway>/gateway
# JUPYTER_GATEWAY_AUTH_TOKEN=<token>

# Platform port (most clouds provide PORT automatically)
PORT=8000
