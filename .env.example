# PowerShell での設定例（起動前に `setx` か、docker-compose に env_file として指定）

# Kernel 有効/無効
PYFLOWS_ENABLE_KERNEL=1
# PYFLOWS_DISABLE_KERNEL=0

# 認証（本番では必須化推奨）
PYFLOWS_API_TOKEN=changeme

# 実行/出力の上限
PYFLOWS_EXEC_TIMEOUT=120
PYFLOWS_EXPORT_MAX_ROWS=200000
PYFLOWS_TIMEOUT_RESTART=1

# データパス
PYFLOWS_DATA_DIR=C:\\data\\flows

# リモートカーネル（任意）
JUPYTER_GATEWAY_URL=
JUPYTER_GATEWAY_AUTH_TOKEN=

# ライセンス（将来）
PYFLOWS_LICENSE_KEY=
PYFLOWS_LICENSE_ALLOW=1# Auth (optional): protect API endpoints
PYFLOWS_API_TOKEN=

# Enable kernel feature (override license gating for dev)
PYFLOWS_ENABLE_KERNEL=1

# Persist flows inside container to /data/flows
PYFLOWS_DATA_DIR=/data/flows

# Optional: use a remote Jupyter Enterprise Gateway
# JUPYTER_GATEWAY_URL=https://<gateway>/gateway
# JUPYTER_GATEWAY_AUTH_TOKEN=<token>

# Platform port (most clouds provide PORT automatically)
PORT=8000
